package org.example.principle1.significantnames;

/**
 * El primer ejemplo corresponde a tener nombres que revelen intencion. Es decir, los nombres deben
 * permitir comprender para que fue creado la variable, funcion, clase, etc.
 */
public class Example1_NamesWithIntention {

    /**
     * Supongamos que tenemos que nombrar tres variables para guardar el dia, mes y año de una
     * fecha de nacimiento
     */

    // Ejemplo de variables que NO revelan intencion
    private int d;
    private int m;
    private int y;

    // Ejemplo de variables que SI revelan intencion
    private int dayOfBirth;
    private int monthOfBirth;
    private int yearOfBirth;

    /**
     * Notar que el nombre de las variables que si revaln intencion, son completamente explicitas sobre la intencion
     * o proposito para las cuales tienes que ser creadas.
     */
}
